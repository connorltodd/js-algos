// Write a function called repeat_str which repeats the given string from the count argument.

// e.g repeat_str(6, "I") "IIIIII"
// e.g. repeat_str(5, "Hello") "HelloHelloHelloHelloHello"

function repeat_str(count, string) {}

console.log(repeat_str(3, "*")); // should return "***"
console.log(repeat_str(5, "#")); // should return "#####"
